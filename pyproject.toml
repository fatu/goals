[project]
name = "goals-sticker"
version = "0.1.0"
description = "A macOS menu bar sticker to track your daily goals and tasks"
readme = "README.md"
requires-python = ">=3.12.2"
dependencies = [
    "rumps>=0.4.0",
]
